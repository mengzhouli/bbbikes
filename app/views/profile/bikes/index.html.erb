<div class="wrapper">
	<div class="container text-center">
		<h1>Your Bikes</h1>
  		<% current_user.bikes.each do |bike| %>
        	<%= render 'bike_card', bike: bike %>
  		<% end %>
	</div>
</div>